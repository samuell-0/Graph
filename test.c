#include "graph_path.h"
#include <stdio.h>
#include <string.h>

int main()
{
    char cmd[SIZE_CMD];
    // memset(cmd, ' ', SIZE_CMD);
    // cmd[SIZE_CMD - 1] = 0;

    // printf("original length: %d\n", strlen(cmd));
    // printf("%s: end here\n", cmd);

    // for (int i = 4; i < 100; i++)
    // {
    //     make_graph_path(cmd, i);
    //     printf("%s: >done\n", cmd);

    // }

    // printf("length: %d\n", strlen(cmd));
    // printf("%s: end here\n", cmd);

    // path_make_cmd(cmd, 17);
    // path_make_cmd(cmd, 28 + 17);
    return 0;
}